{% extends 'base.html.twig' %}

{% block title %} All category {% endblock %}

{% block body %}
<h1> Toutes les categories :</h1>
{% for category in categorys %}
    <div>
        <h2 class="category"><a href="{{ path("category_show", { category: category.id }) }}">{{ category.name }} </a></h2>
        <p><a  class="edit-1" href="{{ path('category_edit', {'id': category.id}) }}">edit</p></a>
 {{ include('category/_delete_form.html.twig') }}</a>

    </div>
{% else %}
    Aucune category trouvé.
    {% endfor %}

    <a href="{{path('app_index') }}">
        retour à l'accueil
        </a>
{% endblock %}