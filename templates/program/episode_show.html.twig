{% extends 'base.html.twig' %}

{% block title %} Series # {{ program.id }} {% endblock %}

{% block body %}

        <p>Titre de la série: {{program.title}} - Saison: {{season.number}}</p>
        <p><img src="{{program.poster}}"/>
        <p> Episode: {{episode.number}}</p>
        <p> Titre de l'épisode: {{episode.title}}</p>
        <p>Résumé en quelques lignes:</p>
        <p>{{episode.synopsis}}</p>
        <p>Temps estimé :{{episode.duration}} minutes</p>

    <p><a href="{{ path("program_season_show", { slug: program.slug, number: season.number }) }}">
        Revenir à la liste des saisons
          

   
    </a><p class="edit" ><a " href="{{ path('app_episode_edit', {'slug': episode.slug}) }}">edit</a></p> 
    <p>{{ include('episode/_delete_form.html.twig') }}</p>
{% endblock %}